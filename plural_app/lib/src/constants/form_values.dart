class FormValues {
  static const passwordMaxLength = 64;
  static const passwordMinLength = 9;

  static const navButtonFadeDuration = Duration(milliseconds: 120);
  static const listedAskTileClickDelay = Duration(milliseconds: 80);
  static const passwordTextRowRevealDuration = Duration(milliseconds: 125);

  // Email
  static const emailMaxLength = 50;
}